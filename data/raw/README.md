# Raw dataset files

Copy the three university-provided CSV files into this directory without renaming them:

- `multinli_train.csv`
- `multinli_validation_matched.csv`
- `multinli_validation_mismatched.csv`

These files are intentionally excluded from Git because they are large.
